import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mrprogrammer.selfaccounting',
  appName: 'SelfAccounting',
  webDir: 'dist'
};

export default config;
